# ========================================
# Streamlit Secrets Configuration (LOCAL)
# ========================================

# Database Configuration
[database]
url = "postgresql://maestro_user:maestro_local_2024@maestro-maestro-postgres-1:5432/maestro"
host = "maestro-maestro-postgres-1"
port = 5432
user = "maestro_user"
password = "maestro_local_2024"
database = "maestro"

# Client Configuration
[client]
default_id = 1

# Grafana Configuration
[grafana]
url = "http://maestro-maestro-grafana-1:3000"
url_public = "http://127.0.0.1:3000"
user = "admin"
password = "maestro2024"

# Loki Configuration
[loki]
url = "http://maestro-maestro-loki-1:3100"

# Prometheus Configuration
[prometheus]
metrics_port = 9464
